// Single source of truth for the version tag shown near the logo (AppLayout
// sidebar) and in the landing page footer. Bump the version — and pick a new
// codename — at the end of a batch that materially changes what the
// platform does, not on every commit; this isn't tied to package.json's
// workspace-internal "0.0.0" placeholder.
export const APP_VERSION = "2.0";
export const APP_VERSION_NAME = "Sentinel";
