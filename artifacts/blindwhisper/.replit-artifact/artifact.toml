kind = "web"
previewPath = "/"
title = "Blind Whisper"
version = "1.0.0"
id = "artifacts/whispick"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22964

[services.development]
run = "pnpm --filter @workspace/blindwhisper run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/blindwhisper", "run", "build" ]
publicDir = "artifacts/blindwhisper/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/app-shell.html"

[services.env]
PORT = "22964"
BASE_PATH = "/"
